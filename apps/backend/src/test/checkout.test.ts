import { describe, it, expect } from "vitest";

describe("Checkout route", () => {
  it("should return 400 on missing body", async () => {
    // mock your handler directly here, or use supertest if Express is running
    expect(true).toBe(true); // stub for now
  });
});
